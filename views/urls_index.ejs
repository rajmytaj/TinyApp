<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <!-- <%= `${url['b2xVn2']}` %> -->
    <% for(const key in url) { %>
      <li> <%= `${key} : ${url[key]}` %> </li>
      <% } %>
</body>
</html>
